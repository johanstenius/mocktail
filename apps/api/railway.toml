[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm db:generate && npx prisma db push && pnpm build"

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
